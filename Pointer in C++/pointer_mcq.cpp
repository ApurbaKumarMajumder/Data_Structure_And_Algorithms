#include<iostream>
using namespace std;

int main(){

    // int first = 8;
    // int second = 18;
    // int *ptr = &second;
    // *ptr = 9;
    // cout<<first<<' '<<second<<endl;

    /*
    int first = 6;
    int *p = &first;
    int *q = p;
    // cout<<q<<endl;
    // cout<<*q<<endl;
    (*q)++;
    cout<<first<<endl;
    */

//    int first = 8;
//    int *p = &first;
//    cout<< (*p)++ << endl;
//    cout<<first<<endl;

    // int first = 8;
    // int second = 11;
    // int *third = &second;
    // first = *third;
    // *third = *third + 2;
    // cout<< first << ' '<<second <<endl;

    // float f = 12.5;
    // float p = 21.5;
    // float* ptr = &f; // address of f
    // (*ptr)++; // value of f
    // *ptr = p;
    // cout<< *ptr << ' ' << f << " " << p <<endl;

    return 0;
}